#include <stdio.h>
 /**
 * Author: Femi Akintan
 * Program: WinMingle Community C Training
 * Description: Printing all posible combination of  two digit numbers
 */

int main(void){
    int n;
    for (n = 10; n <= 99; n++){
     putchar((n / 10) + '0');
     putchar((n % 10) + '0');
      if(n != 99)
      putchar(',');
    }
  putchar('\n');
    return (0);
}
